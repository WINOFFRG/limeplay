import { createStyles } from '@mantine/styles';

const useStyles = createStyles((theme) => ({
	iconStyle: {
		width: '24px',
		height: '24px',
		margin: 'auto',
		pointerEvents: 'none',
	},
}));

export default useStyles;
