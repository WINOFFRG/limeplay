import React from 'react';
import { DEFAULT_THEME } from '@mantine/core';
import { IconCode, IconStar, IconRocket, IconSearch } from '@tabler/icons';

export default [
    {
        to: '/pages/about',
        label: 'About Project',
        color: DEFAULT_THEME.colors.yellow[5],
        icon: IconSearch,
    },
];
