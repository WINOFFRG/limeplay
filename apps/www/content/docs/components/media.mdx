---
title: Media Element
description: Native HTML Media Element (Audio | Video) mounted to store
---
import { Step, Steps } from 'fumadocs-ui/components/steps';

## Installation

<Steps>
<Step>
```npm
npx shadcn add @limeplay/media
```
</Step>

<Step>
### Setup the provider in the root

```tsx lineNumbers title="components/player/media-player.tsx"
import { Media } from "@/components/limeplay/media" // [!code ++]

// ... other imports

export function MediaPlayer() {
  return (
    <MediaProvider>
      <PlayerHooks />
      <Suspense>
        <Layout.RootContainer height={720} width={1280}>
          {/* [!code ++] */}
          <Media
            as="video"
            autoPlay={true}
            className="size-full object-cover"
            muted
          />
        </Layout.RootContainer>
      </Suspense>
    </MediaProvider>
  )
}
```

</Step>
</Steps>

## Understanding

Native HTML Media Element component that provides audio and video playback capabilities. It supports both `video` and `audio` elements via the `as` prop.

## Components

### Media

The main component that renders either a `<video>` or `<audio>` element based on the `as` prop.

#### API Reference

<AutoTypeTable path="./registry/default/ui/media.tsx" name="MediaPropsDocs" />
